Locale.setDefault(locale.us); -- Configurar em determinada região.
scanner de dados;
next-> texto
nextint-> numero inteiro
nextDouble-> numero double
next().charAt(0)-> Caracteres
nextLine()->quebra de linha;

math.sqrt(X) raiz quadrada do x
Math.pow(x,y) potencia de x elevado ao Y
Math.abs(25.0)Variável A recebe o valor absoluto de x

(condição) ? valor_verdadeiro : valor falso

.toLowerCase() -- Letra minuscula
l.toUpperCase(); -- letra maiuscula
trim(); remove branco
.substring(2) recuperar a letra da posição
substring(2, 9); recupera nas duas posição
replace('a', 'x') substituição
replace("abc", "xy") subistuir na string na primeira pelo segunda
.indexOf("bc") primeira posição do dado;
lastIndexOf("bc"); ultima lastIndexOf("bc");
split quebra a string em determinada posição

publix static int max(int x, int y, int z) quando recebe variaveis;

função que recebe vazio void;
publix static void (variavel que recebe) função que nao retorna nada;